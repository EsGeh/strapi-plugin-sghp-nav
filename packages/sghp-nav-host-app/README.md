# Strapi dummy Host for the sghp-nav Plugin

This is a dummy Strapi application, because in order to build the plugin for publishing on npm, its "peerDependencies" must be fulfilled.

# Build Plugin

    $ yarn
    $ npm run buildplugins
